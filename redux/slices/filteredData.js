import speciesData from "../../public/allDataByAssessmentId.json"

const filteredData = (state = speciesData, action) => {
   switch(action.type) {
      default:
         return state
   }
}

export default filteredData